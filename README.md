A static webpage displaying a personal poetry collection stored in git-tracked [.poem files](https://github.com/bbkingisking/poem-schema).

This could be you if you are storing your poetry collection on any git provider and you set the `POETRY_REPO_URL=` env var to point to it. 
