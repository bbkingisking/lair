{
  "name": "lair",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "predev": "npm run fetch-poems && npm run optimize-fonts",
    "dev": "astro dev",
    "optimize-fonts": "node scripts/optimize-fonts.mjs",
    "fetch-poems": "node scripts/fetch-poems.mjs",
    "prebuild": "npm run fetch-poems && npm run optimize-fonts",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.2.2",
    "iso-639-3": "^3.0.1"
  }
}
